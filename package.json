{
  "name": "@parmanasystems/openai-refund-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Real OpenAI-powered refund agent: proposes refunds from natural language, but Parmana is the sole authorization authority.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint .",
    "start": "node --import tsx src/server/index.ts"
  },
  "dependencies": {
    "openai": "7.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^3.0.0"
  }
}
